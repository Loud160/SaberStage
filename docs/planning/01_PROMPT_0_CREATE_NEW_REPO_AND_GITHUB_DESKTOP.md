# Prompt 0 — Create the SaberStage repository and connect it to GitHub Desktop

Create the new local repository for the project described in `00_PROJECT_CHARTER.md`.

This phase is repository placement and planning-document preservation only.

## Required result

1. Create `C:\Users\Owner\source\repos\SaberStage`.
2. Initialize Git with `main` as the default branch.
3. Copy the complete planning prompt set into:

   ```text
   docs/planning/
   ```

4. Preserve the downloaded source documents unchanged.
5. Do not add mod scaffolding, implementation, dependencies, project hygiene files, a license, or a root project README in this phase.
6. Add the local repository to GitHub Desktop and verify it appears as `SaberStage`.
7. Do not publish or create a GitHub remote unless explicitly authorized.
8. Do not commit unless explicitly requested.
9. Show the repository tree and `git status`.

Stop after the local repository, planning documents, and GitHub Desktop link are verified.
